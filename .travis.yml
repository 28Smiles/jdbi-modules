language: java

sudo: false

env:
  global:
  - COVERALLS_REPO_TOKEN=mySecRetRepoToken

matrix:
  include:
  - script:
    - ci/ci-test.sh
    jdk: oraclejdk10
  - script:
    - ci/ci-test.sh
    jdk: oraclejdk11